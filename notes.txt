python -m venv env -> creates env

source env/bin/activate -> activates env
deactivate              -> deactivates env

pip install flask 	-> installs flask in env
flask run         	-> starts flask

flask db migrate    -> Run when you make class changes
&& flask db upgrade

pip freeze -> Lists installed pip packages

echo -n hello | sha256sum -> Generates hash of hello in Konsole

git commit -m "message" -> commit with a message
git add . 		-> stage it i think. YOU HAVE TO DO THIS BEFORE COMMITTING
&& 			-> back to back commands
git push --force -> Destructive, pushes reguardless of files missing

---In Order---
add, commit, and stage

git add . && git commit -m "Be descriptive"
git push
